/* eslint-disable react-hooks/exhaustive-deps */
import type { NextPage } from "next";
import EventList from "@/components/page/EventListPage";

const Home: NextPage = () => {
  return <EventList />;
};

export default Home;
